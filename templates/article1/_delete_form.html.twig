<form method="post" action="{{ path('app_article1_delete', {'id': article1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article1.id) }}">
    <button class="btn">Delete</button>
</form>
