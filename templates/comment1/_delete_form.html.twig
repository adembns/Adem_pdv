<form method="post" action="{{ path('app_comment1_delete', {'id': comment1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment1.id) }}">
    <button class="btn">Delete</button>
</form>
