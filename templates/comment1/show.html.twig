{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
    <h1>Comment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment1.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ comment1.contenu }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ comment1.created ? comment1.created|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ comment1.updated ? comment1.updated|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comment1_index') }}">back to list</a>

    <a href="{{ path('app_comment1_edit', {'id': comment1.id}) }}">edit</a>

    {{ include('comment1/_delete_form.html.twig') }}
{% endblock %}
